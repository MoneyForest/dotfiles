# Development Tools

## gitignore Generation

Use [gibo](https://github.com/simonwhitaker/gibo) to generate `.gitignore` files:

```bash
# Example: Generate gitignore for Go project
gibo dump Go >> .gitignore
```
